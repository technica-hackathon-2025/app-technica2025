export default function Dashboard(setUser: any) {
    return (
        <div>
            <h1>Welcome to the Dashboard</h1>
            <p>This is a protected route accessible only to authenticated users.</p>
        </div>
    );
}